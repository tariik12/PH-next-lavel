// Normalization

    // Functional Dependency
            // Functional dependency in simple terms means that the value of one attribute (or set of attributs)uniquely determines the value of another attribute(s)in a database table
    // Normal Forms
