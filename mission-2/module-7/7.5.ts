// structured Query Language(SQL)
// we can communicate with rdb using sql;
//Query/ SQL Statement => close/key work, identifier
// language = declarative & imparative
// sql is a declarative language

// sql commands
    // DDL(DATA DEFNATION LANGUAGE) = CREATE, DROP, ALTER, TRUNCATE
    // DML(DATA MANIPULATION/MODIFICATION LANGUAGE) = INSERT, UPDATE, DELETE
    // DCL(DATA CONTROL LANGUAGE) = GRANT, REVOKE
    // TCL(TRANGACTION CONTROL LANGUAGE) = COMMIT, ROLLBACK, SAVEPOINT
    // DQL(DATA QUERY LANGUAGE) = SELECT